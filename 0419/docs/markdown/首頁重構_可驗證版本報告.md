# 首頁重構 - 可驗證版本報告

**日期**: 2026年3月27日  
**報告等級**: ⚠️ **初步審查版（不含實測數據）**  
**編寫原則**: 區分「程式碼改動（可驗證）」vs「推測改善（待實測）」

---

## 📋 一、已完成且可被程式碼驗證的修改

### ✅ 1. HTML 修改

| 項目 | 修改前 | 修改後 | 程式碼位置 | 可見性 |
|------|--------|--------|----------|--------|
| **floatObserver 刪除** | `new IntersectionObserver` 觀察浮動元素 | 完全刪除 floatObserver | index.html script 標籤內 | ❌ 不可見（無動畫移除） |
| **圖片 lazy loading** | 無 loading 屬性 | `loading="lazy"` x7 - Journey 3張 + Feature 4張 | index.html 各圖片標籤 | ✅ 程式碼驗證可見 |
| **UnifiedChat 延遲** | `setTimeout(initUnifiedChat, 800)` | `setTimeout(initUnifiedChat, 3000)` | index.html DOMContentLoaded 內 | ❌ 不可見（邏輯層） |

### ✅ 2. CSS 修改 - Hero 區

| 項目 | 修改前 | 修改後 | 理由 | 可驗證程度 |
|------|--------|--------|------|-----------|
| **hero-orb 顯示** | `display: none` | `display: block` | 恢復靜態視覺裝飾 | ✅ 可見 (視覺改動) |
| **hero-orb 動畫** | (無) | (無) | 保留靜態，無 animation 屬性 | ✅ 確認無動畫 |
| **Hero Panel 背景** | `rgba(..., 0.95)` | `rgba(..., 0.98)` | 提亮背景亮度 | ✅ 視覺微妙 |
| **Hero Panel 邊框** | `rgba(..., 0.2)` | `rgba(..., 0.28)` | 增強邊框視覺 | ✅ 視覺改動 |
| **Hero Panel 陰影** | `0 8px 16px rgba(0, 0, 0, 0.12)` | `0 8px 20px rgba(0, 0, 0, 0.16)` | 加強立體感 | ✅ 視覺改動 |

### ✅ 3. CSS 修改 - 動畫區

| 項目 | 修改前 | 修改後 | 狀態 | 備註 |
|------|--------|--------|------|------|
| **Spinner 動畫** | `animation: pulse-ring 2s infinite` | `animation: saas-spin 1.1s linear infinite, pulse-ring 2s infinite` | ⚠️ 恢復旋轉 | **問題**: 2 個 infinite 疊加 |
| **Float 動畫** | `animation: none` | `animation: floatY 3s ease-in-out infinite` | ⚠️ 改為 3s | **問題**: 仍然 infinite |
| **Float Delay** | `animation: none` | `animation: floatY 3.2s ease-in-out infinite; animation-delay: 0.2s` | ⚠️ 改為 3.2s | **問題**: 仍然 infinite |

### ✅ 4. CSS 修改 - 功能卡區

| 項目 | 修改前 | 修改後 | 是否改善首頁流暢度 | 已實測 |
|------|--------|--------|--------------|--------|
| **Feature Card 背景** | `rgba(255, 255, 255, 0.9)` | `rgba(255, 255, 255, 0.93)` | ❌ (視覺層，不影響性能) | ❌ |
| **Feature Card 邊框** | `rgba(59, 130, 246, 0.08)` | `rgba(59, 130, 246, 0.14)` | ❌ (視覺層) | ❌ |
| **Feature Card 陰影** | `0 4px 12px rgba(0, 0, 0, 0.04)` | `0 8px 20px rgba(0, 0, 0, 0.08)` | ⚠️ (陰影複雜度略增) | ❌ |
| **Feature Card Hover** | `transform: translateY(-2px)` | `transform: translateY(-4px)` | ⚠️ (觸發 Repaint) | ❌ |
| **Feature Card Hover 陰影** | `0 6px 16px rgba(0, 0, 0, 0.06)` | `0 12px 28px rgba(0, 0, 0, 0.12)` | ⚠️ (陰影複雜度增) | ❌ |
| **Feature Image Hover** | `transform: scale(1.02)` | `transform: scale(1.08)` | ⚠️ (更大的縮放) | ❌ |

### ✅ 5. CSS 修改 - Journey 卡區

| 項目 | 修改前 | 修改後 | 理由 | 視覺影響 |
|------|--------|--------|------|---------|
| **Journey Card 邊框** | `rgba(59, 130, 246, 0.12)` | `rgba(59, 130, 246, 0.2)` | 增強定義度 | ✅ 更明顯 |
| **Journey Card 陰影** | `0 4px 12px rgba(0, 0, 0, 0.04)` | `0 8px 20px rgba(0, 0, 0, 0.08)` | 加強立體感 | ✅ 更深 |
| **Journey Card Hover** | `transform: translateY(-2px)` | `transform: translateY(-4px)` | 互動反饋 | ✅ 更明顯 |

### ✅ 6. CSS 修改 - 背景光暈

| 項目 | 修改前 | 修改後 | GPU 成本? |
|------|--------|--------|-----------|
| **Feature Visual Blur** | `filter: blur(30px)` | `filter: blur(40px)` | ⬆️ (增加) |
| **Feature Visual 漸變** | `rgba(86, 243, 255, 0.12)` | `rgba(86, 243, 255, 0.2)` | ⬆️ (更不透明) |

---

## 🔍 二、預期改善但尚未實測的項目

以下內容**基於程式碼邏輯推估**，尚未進行實機或 DevTools 實測。

### 🔸 預期被改善的項目

| 項目 | 預期改善 | 理論依據 | 實測狀態 |
|------|---------|---------|---------|
| **JS 首屏初始化時間** | ⬇️ 削減 ~1-2 秒 | floatObserver 移除 + UnifiedChat 延遲 3s | ❌ **未實測** |
| **首屏 IntersectionObserver 實例** | ⬇️ 從 2 個 → 1 個 | 移除 floatObserver | ❌ **未實測** |
| **圖片初始請求體積** | ⬇️ -25% | 7 張圖片使用 lazy loading | ❌ **未實測** |
| **首頁並行下載請求數** | ⬇️ 削減 | 非首屏圖片延遲加載 | ❌ **未實測** |
| **CSS 著色複雜度 (Painting)** | ⬆️ 略增 | 陰影從單層改為更複雜、hover 抬起增大 | ❌ **未實測** |
| **GPU 動畫層 (Composite)** | 📊 不清楚 | Spinner 和 Float 仍為 infinite | ❌ **未實測** |
| **滾動 FPS 穩定性** | ❓ 不確定 | 無 float 浮動觀察，但新增的 hover 陰影可能抵消 | ❌ **未實測** |

### 🔸 預期**沒有改善**的項目

| 項目 | 說明 | 理由 |
|------|------|------|
| **Spinner 持續動畫成本** | 仍然存在 | `saas-spin 1.1s linear infinite` 保留 |
| **Float 持續動畫成本** | 仍然存在 | `floatY 3s/3.2s infinite` 保留 |
| **脈衝環動畫成本** | 仍然存在 | `pulse-ring 2s infinite` 保留 |
| **非首屏選擇器污染** | 可能仍存在 | 未清理不必要的全域樣式 |

---

## 🧪 三、已實測且有依據的項目

**當前狀態**: ⚠️ **無實測依據**

尚未執行以下測試：
- Chrome DevTools Performance 錄製
- Lighthouse 審計
- 實機手機滾動測試
- 網路限流測試
- CPU 節流測試

---

## ⚠️ 四、首頁殘留風險清單

### 4.1 動畫層疊問題

| 風險 | 狀態 | 說明 |
|------|------|------|
| **Spinner 雙動畫** | ⚠️ 仍存在 | `saas-spin 1.1s infinite` + `pulse-ring 2s infinite` 同時運行 |
| **Float 仍為 Infinite** | ⚠️ 仍存在 | `floatY 3s infinite` - 在滾動期間持續變換 |
| **Feature Hover 動畫堆疊** | ⚠️ 仍存在 | hover 時同步觸發 `transform` + `filter` + `box-shadow` |
| **Reveal Animation** | ✅ 保留 (需要) | 進場時單次動畫，無持續成本 |

### 4.2 CSS 複雜度增加

| 風險項 | 修改前 | 修改後 | 影響 |
|--------|--------|--------|------|
| **Feature Card 陰影** | `0 4px 12px` (單層) | `0 8px 20px` (複雜度增) | Painting 成本↑ |
| **Feature Visual Blur** | 30px | 40px | GPU Blur Filter 成本↑ |
| **Hover 陰影** | `0 6px 16px` | `0 12px 28px` | 更複雜的陰影計算 |
| **Hover Transform** | -2px | -4px | Repaint 面積增大 |

### 4.3 未清理的選擇器

| 選擇器 | 狀態 | 備註 |
|--------|------|------|
| `body.home-saas .floatObserver` | ❌ 可能仍存在 CSS | floatObserver 已刪除，但 CSS 規則未清 |
| 其他頁面全域 `.hover` 樣式 | ⚠️ 可能污染 | 首頁未隔離全域 hover 規則 |
| `backdrop-filter` 全域應用 | ⚠️ 可能仍存在 | 首頁 header 仍可能有過重濾鏡 |

### 4.4 圖片最佳化

| 項目 | 狀態 | 說明 |
|------|------|------|
| **圖片格式** | ⚠️ 未最佳化 | 仍為 .png，未轉 WebP |
| **圖片尺寸** | ⚠️ 未最佳化 | 未確認圖片是否過大 |
| **srcset 屬性** | ❌ 缺失 | 無響應式圖片設定 |

---

## 📊 五、修改前後對照表（完整版）

### 總覽

| 修改類別 | 修改項數 | 已實測 | 可見性 | 風險等級 |
|---------|---------|--------|--------|---------|
| **HTML DOM** | 2 (lazy loading + UnifiedChat) | ❌ | 程式碼層 | 🟢 低 |
| **CSS 視覺** | 8+ (顏色、邊框、陰影) | ❌ | 可見 | 🟢 低 |
| **CSS 動畫** | 3 (spinner、float、hover) | ❌ | 可見 | 🟡 中 |
| **JS 邏輯** | 2 (移除觀察者、延遲初始化) | ❌ | 邏輯層 | 🟢 低 |

### 逐項對照

```
┌─────────────────────────────────────────────────────┐
│                   修改項目對照表                     │
├─────────┬──────────┬──────────┬────────┬────────────┤
│ 項目    │ 修改前   │ 修改後   │ 改善類 │ 實測狀態   │
├─────────┼──────────┼──────────┼────────┼────────────┤
│ Orb顯示 │ none     │ block    │ 視覺✓  │ ❌ 未測   │
│ Spinner │ 脈衝環   │ 旋+脈衝  │ 視覺✓  │ ❌ 未測   │
│         │          │ (2個∞)   │ 性能✗  │            │
├─────────┼──────────┼──────────┼────────┼────────────┤
│ Float   │ none     │ 3s (∞)   │ 視覺✓  │ ❌ 未測   │
│         │          │          │ 性能△  │            │
├─────────┼──────────┼──────────┼────────┼────────────┤
│ 卡片陰影│ 4px 12px │ 8px 20px │ 視覺✓  │ ❌ 未測   │
│         │          │          │ 性能✗  │            │
├─────────┼──────────┼──────────┼────────┼────────────┤
│ Lazy    │ (無)     │ x7       │ 性能✓  │ ❌ 未測   │
│ loading │          │          │        │            │
├─────────┼──────────┼──────────┼────────┼────────────┤
│ Blur    │ 30px     │ 40px     │ 視覺✓  │ ❌ 未測   │
│         │          │          │ 性能✗  │            │
│
└─────────┴──────────┴──────────┴────────┴────────────┘
```

---

## 🔧 六、下一步建議

### 🟡 優先級 1（立即行動）

1. **執行 Lighthouse 審計**
   - 分別測試修改前後版本
   - 對比 Performance 分數、FCP、LCP、CLS
   - 命令: `lighthouse http://localhost:3000 --view`

2. **Chrome DevTools Performance 錄製**
   - 記錄首頁載入 3 秒內的性能曲線
   - 檢查 Spinner + Float 是否持續觸發 Composite
   - 檢查 Hover 時的 Painting 成本

3. **清理 Spinner 雙動畫問題**
   ```css
   /* 建議：改為單動畫 */
   body.home-saas .decision-spinner {
       animation: saas-spin 1.1s linear infinite; /* 移除 pulse-ring */
       /* 改用 box-shadow 靜態脈衝效果 */
   }
   ```

4. **檢查 Float Infinite 必要性**
   - Float 動畫是否必須要 infinite？
   - 或改為一次性動畫（如進場動畫）
   - 或改為 hover 觸發的動畫

### 🟢 優先級 2（後續最佳化）

5. **清理未使用的 CSS 規則**
   ```bash
   # 使用 PurgeCSS 或手動檢查
   - 移除 .floatObserver 相關規則
   - 檢查 body.home-saas 覆蓋範圍
   ```

6. **圖片最佳化**
   ```bash
   - 轉換 .png → .webp
   - 添加 srcset 屬性
   - 驗證圖片尺寸是否超過實際顯示尺寸
   ```

7. **Hover 狀態隔離**
   ```css
   /* 確保 hover 效果被隔離到首頁 */
   body.home-saas .feature-card-late:hover { /* 已隔離 */ }
   ```

### 🔵 優先級 3（驗證後執行）

8. **實機測試**
   - iPhone 12+ 滑動測試
   - Android 中端機型測試
   - 網路限流 (4G 模擬) 測試

9. **建立性能基準線**
   - 記錄修改前的性能指標
   - 記錄修改後的性能指標
   - 建立 CI/CD 性能檢查

---

## 📝 七、驗證檢查清單

### 程式碼層驗證（已完成 ✅）

- [x] floatObserver 代碼刪除
- [x] lazy loading 屬性已添加
- [x] UnifiedChat 延遲到 3s
- [x] Hero Orb 改為可見
- [x] CSS 邊框、背景、陰影已調整
- [x] 動畫屬性已修改

### 性能層驗證（待執行 ❌）

- [ ] Lighthouse 分數對比
- [ ] DevTools Performance 錄製
- [ ] CPU 節流測試
- [ ] 網路限流測試
- [ ] 實機滑動 FPS 測試
- [ ] 首次互動延遲測試 (FID / INP)

### 視覺層驗證（已完成 ✅）

- [x] Hero Orb 可見
- [x] 陰影增強可見
- [x] Spinner 旋轉可見
- [x] Float 動畫可見
- [x] Hover 效果可見

---

## ⚖️ 八、風險評估

### 視覺美感 vs 性能的平衡

| 方面 | 狀態 | 評估 |
|------|------|------|
| **首頁美感** | ✅ 恢復 | Hero Orb、動畫、陰影都恢復了 |
| **性能最佳化程度** | ⚠️ 部分 | 移除 floatObserver + lazy loading 有幫助，但仍保留 infinite 動畫 |
| **實測驗證** | ❌ 缺乏 | 無 DevTools 或 Lighthouse 數據支持 |
| **使用者體驗** | ❓ 不確定 | 需實測後才能確認是否改善 |

---

## 📌 結論

### 現況
✅ **視覺層**: 程式碼修改已完成，Hero + 動畫 + 卡片樣式都恢復  
✅ **邏輯層**: floatObserver 移除、UnifiedChat 延遲、lazy loading 添加  
❌ **性能層**: 無實測數據支持任何性能聲稱  
⚠️ **風險層**: 仍保留 2 個 infinite 動畫 + 複雜陰影

### 能說的
- "我們移除了 floatObserver 觀察者"
- "我們添加了 7 張圖片的 lazy loading"
- "我們將 UnifiedChat 初始化延遲到 3 秒"
- "我們恢復了視覺美感（Orb、動畫、陰影）"

### 不能說的
- ❌ "首頁性能快 70%"（沒測試）
- ❌ "FPS 58-60"（沒測試）
- ❌ "首次互動延遲下降 60%"（沒測試）
- ❌ "GPU 占用下降 50%"（沒測試）

### 下一個里程碑
在執行 Lighthouse + DevTools 測試之前，此報告為 **初步審查版**
建議在獲得實測數據後重新發布 **最終驗證版**

---

**報告簽名**: 可驗證版本 v1.0  
**下一版本**: 待實測數據補充後發布 v2.0 (最終驗證版)

